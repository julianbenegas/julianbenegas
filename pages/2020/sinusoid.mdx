import Layout from '../../components/mdx/Layout'
import Sinusoid from '../../components/Sinusoid'
export default ({ children }) => (
  <Layout pagetitle="Sinusoid">{children}</Layout>
)

# Sinusoid

When struggling, we introspect — work on ourselves.

When winning, we see it as a waste of time.

<Sinusoid />
