import Layout from '../../components/mdx/Layout'
export default ({ children }) => <Layout pagetitle="Flow">{children}</Layout>

# Flow

When performing

Your fear of failing<br />
Your excitement because of<br />
a possible success

They don't matter<br />
Only the mission matters

When it ends, yes<br />
Cry and learn<br />
And celebrate
